---
/**
 * Hero with non-hero backdrop image (served from /public for reliability).
 * - Backdrop uses absolute path /img/hero03.png
 * - Foreground/CTA lifted with z-10/z-20 above the backdrop
 */
const srTitle = "Build sharp websites with Astro + Tailwind";
---
<section aria-labelledby="hero-heading" class="relative w-full min-h-[100svh] flex items-start px-5 md:px-7 lg:px-8 py-24 lg:pt-12 xl:pt-16 2xl:pt-24">
  <!-- Foreground content container (above backdrop) -->
  <div class="relative z-10 w-full max-w-[1600px] mx-auto grid auto-rows-min auto-cols-fr gap-x-6">
    <h1 id="hero-heading" class="sr-only">{srTitle}</h1>

    <div class="col-span-full">
      <p class="text-balance">
        <span class="block font-extrabold tracking-tight text-5xl md:text-6xl lg:text-[84px] 2xl:text-[140px]/[0.95]">
          Build sharp websites
        </span>
        <span class="block font-medium tracking-tight opacity-90 text-2xl md:text-3xl lg:text-5xl 2xl:text-6xl">
          with Astro + Tailwind tokens
        </span>
      </p>
    </div>
  </div>

  <!-- Backdrop layer (decorative, non-hero): fixed behind content -->
  <div aria-hidden="true" class="pointer-events-none fixed inset-0 z-0 overflow-hidden">
    <img
      src="/img/hero03.png"
      alt=""
      decoding="async"
      loading="eager"
      class="w-full h-full object-cover object-center select-none"
      draggable="false"
    />
  </div>

  <!-- CTA bar: mobile near bottom via top:calc(85svh-2rem); on lg+ switch to bottom:25svh; lifted above backdrop -->
  <div class="absolute inset-x-0 top-[calc(85svh-2rem)] lg:top-auto lg:bottom-[25svh] px-5 md:px-7 lg:px-8 z-20">
    <div class="w-full max-w-[1600px] mx-auto flex items-center justify-between gap-4">
      <a href="#work" class="pointer-events-auto inline-flex items-center gap-2 underline underline-offset-4 font-medium focus:outline-none focus:ring-2 focus:ring-offset-2">
        See work
      </a>
      <a href="/contact" class="pointer-events-auto inline-flex items-center gap-2 rounded-xl px-4 py-2 border focus:outline-none focus:ring-2 focus:ring-offset-2">
        Start a project
      </a>
    </div>
  </div>
</section>
